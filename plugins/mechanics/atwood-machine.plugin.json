{
  "name": "Máquina de Atwood",
  "version": "1.0.0",
  "author": "Physics Lab Team",
  "description": "Simulação realista da Máquina de Atwood com roldana giratória, corda contínua, inércia e atritos ajustáveis",
  "category": "mechanics",
  "objects": [
    {
      "id": "atwood_pulley",
      "name": "Roldana Atwood",
      "icon": "settings",
      "type": "shape",
      "defaultProps": {
        "x": 550,
        "y": 150,
        "w": 48,
        "mass": 2,
        "momentOfInertia": 0.5,
        "friction": 0.02,
        "ropeFriction": 0.01,
        "fill": "#e2e8f0",
        "stroke": "#475569",
        "lineWidth": 2,
        "static": true,
        "collidable": true,
        "leftMass": 1,
        "rightMass": 1.2,
        "leftPosition": 420,
        "rightPosition": 420,
        "ropeColor": "#5b21b6",
        "ropeWidth": 3,
        "velocity": 0,
        "angularVelocity": 0,
        "rotationAngle": 0
      },
      "draw": "function(ctx, obj) {\n  ctx.save();\n  \n  // Aplicar rotação para animação\n  if (obj.rotationAngle) {\n    ctx.translate(obj.x, obj.y);\n    ctx.rotate(obj.rotationAngle);\n    ctx.translate(-obj.x, -obj.y);\n  }\n  \n  // Círculo externo da roldana\n  ctx.beginPath();\n  ctx.arc(obj.x, obj.y, obj.w, 0, Math.PI * 2);\n  ctx.fillStyle = obj.fill;\n  ctx.fill();\n  ctx.strokeStyle = obj.stroke;\n  ctx.lineWidth = obj.lineWidth;\n  ctx.stroke();\n  \n  // Canal da corda (rebaixo)\n  ctx.beginPath();\n  ctx.arc(obj.x, obj.y, obj.w - 6, 0, Math.PI * 2);\n  ctx.fillStyle = '#1e293b';\n  ctx.fill();\n  \n  // Eixo central\n  ctx.beginPath();\n  ctx.arc(obj.x, obj.y, obj.w * 0.2, 0, Math.PI * 2);\n  ctx.fillStyle = '#0f172a';\n  ctx.fill();\n  ctx.beginPath();\n  ctx.arc(obj.x, obj.y, obj.w * 0.12, 0, Math.PI * 2);\n  ctx.fillStyle = '#facc15';\n  ctx.fill();\n  \n  // Raios (6 raios para indicar rotação)\n  ctx.beginPath();\n  for (let i = 0; i < 6; i++) {\n    const angle = (i * Math.PI * 2) / 6;\n    const innerX = obj.x + Math.cos(angle) * (obj.w * 0.25);\n    const innerY = obj.y + Math.sin(angle) * (obj.w * 0.25);\n    const outerX = obj.x + Math.cos(angle) * (obj.w - 8);\n    const outerY = obj.y + Math.sin(angle) * (obj.w - 8);\n    ctx.moveTo(innerX, innerY);\n    ctx.lineTo(outerX, outerY);\n  }\n  ctx.strokeStyle = '#475569';\n  ctx.lineWidth = 1.5;\n  ctx.stroke();\n  \n  ctx.restore();\n  \n  // Desenhar corda contínua (conectando as duas massas)\n  if (obj.leftMassObj && obj.rightMassObj) {\n    ctx.save();\n    ctx.beginPath();\n    \n    const leftMassTop = obj.leftMassObj.y;\n    const rightMassTop = obj.rightMassObj.y;\n    const pulleyTop = obj.y - obj.w * 0.6;\n    \n    // Ponto de contato esquerdo da roldana\n    const leftContactX = obj.x - obj.w * 0.55;\n    const leftContactY = obj.y + obj.w * 0.15;\n    \n    // Ponto de contato direito da roldana\n    const rightContactX = obj.x + obj.w * 0.55;\n    const rightContactY = obj.y + obj.w * 0.15;\n    \n    // Corda esquerda: da massa até a roldana\n    ctx.moveTo(obj.leftMassObj.x, leftMassTop);\n    ctx.lineTo(leftContactX, leftContactY);\n    \n    // Corda superior (sobre a roldana)\n    ctx.moveTo(leftContactX, leftContactY);\n    ctx.quadraticCurveTo(obj.x, obj.y - 5, rightContactX, rightContactY);\n    \n    // Corda direita: da roldana até a massa\n    ctx.moveTo(rightContactX, rightContactY);\n    ctx.lineTo(obj.rightMassObj.x, rightMassTop);\n    \n    ctx.strokeStyle = obj.ropeColor;\n    ctx.lineWidth = obj.ropeWidth;\n    ctx.lineCap = 'round';\n    ctx.stroke();\n    ctx.restore();\n  }\n}",
      "update": "function(obj, dt) {\n  if (!obj.leftMassObj || !obj.rightMassObj) return;\n  \n  const m1 = obj.leftMass;\n  const m2 = obj.rightMass;\n  const I = obj.momentOfInertia;\n  const r = obj.w / 100;\n  const g = Lab.state.gravity;\n  const friction = obj.friction;\n  const ropeFriction = obj.ropeFriction;\n  \n  // Aceleração do sistema (considerando inércia da roldana)\n  const denominator = m1 + m2 + I / (r * r);\n  let a = (m2 - m1) * g / denominator;\n  \n  // Atrito dinâmico (opõe-se ao movimento)\n  if (Math.abs(obj.velocity) > 0.001) {\n    const frictionForce = friction * (m1 + m2) * g;\n    const frictionAcc = frictionForce / denominator;\n    if (a > 0) a = Math.max(0, a - frictionAcc);\n    else if (a < 0) a = Math.min(0, a + frictionAcc);\n  }\n  \n  // Atrito de rolamento da roldana\n  const angularFriction = Math.abs(obj.angularVelocity) * ropeFriction;\n  \n  // Atualizar velocidades\n  obj.velocity = (obj.velocity || 0) + a * dt;\n  obj.velocity *= (1 - friction * dt * 0.3);\n  \n  obj.angularVelocity = (obj.angularVelocity || 0) + (a / r) * dt;\n  obj.angularVelocity *= (1 - angularFriction * dt);\n  \n  // Ângulo de rotação visual\n  obj.rotationAngle = (obj.rotationAngle || 0) + obj.angularVelocity * dt * 30;\n  \n  // Atualizar posições das massas\n  let leftNewY = (obj.leftPosition || 420) - obj.velocity * dt;\n  let rightNewY = (obj.rightPosition || 420) + obj.velocity * dt;\n  \n  // Limites para não sair da tela\n  const minY = obj.y + obj.w + 15;\n  const maxY = 620;\n  leftNewY = Math.max(minY, Math.min(maxY, leftNewY));\n  rightNewY = Math.max(minY, Math.min(maxY, rightNewY));\n  \n  obj.leftPosition = leftNewY;\n  obj.rightPosition = rightNewY;\n  \n  // Atualizar os objetos das massas\n  if (obj.leftMassObj) {\n    obj.leftMassObj.y = obj.leftPosition;\n  }\n  if (obj.rightMassObj) {\n    obj.rightMassObj.y = obj.rightPosition;\n  }\n}",
      "properties": [
        { "name": "leftMass", "label": "Massa Esquerda (kg)", "type": "number", "default": 1, "min": 0.1, "max": 5, "step": 0.1 },
        { "name": "rightMass", "label": "Massa Direita (kg)", "type": "number", "default": 1.2, "min": 0.1, "max": 5, "step": 0.1 },
        { "name": "momentOfInertia", "label": "Inércia da Roldana", "type": "range", "default": 0.5, "min": 0.1, "max": 1.5, "step": 0.05 },
        { "name": "friction", "label": "Atrito da Corda", "type": "range", "default": 0.02, "min": 0, "max": 0.2, "step": 0.01 },
        { "name": "ropeFriction", "label": "Atrito da Roldana", "type": "range", "default": 0.01, "min": 0, "max": 0.15, "step": 0.005 },
        { "name": "w", "label": "Raio da Roldana (px)", "type": "number", "default": 48, "min": 30, "max": 70, "step": 2 },
        { "name": "ropeColor", "label": "Cor da Corda", "type": "color", "default": "#5b21b6" }
      ]
    },
    {
      "id": "atwood_mass",
      "name": "Massa Atwood",
      "icon": "circle",
      "type": "shape",
      "defaultProps": {
        "w": 28,
        "mass": 1,
        "fill": "#3b82f6",
        "stroke": "#1d4ed8",
        "lineWidth": 2,
        "collidable": true
      },
      "draw": "function(ctx, obj) {\n  ctx.save();\n  \n  // Sombra\n  ctx.shadowBlur = 6;\n  ctx.shadowColor = 'rgba(0,0,0,0.25)';\n  \n  // Corpo da massa (formato de cilindro)\n  ctx.beginPath();\n  ctx.ellipse(obj.x, obj.y, obj.w, obj.w * 0.85, 0, 0, Math.PI * 2);\n  \n  // Gradiente para efeito 3D\n  const grad = ctx.createLinearGradient(obj.x - obj.w * 0.4, obj.y - obj.w * 0.3, obj.x + obj.w * 0.4, obj.y + obj.w * 0.5);\n  grad.addColorStop(0, obj.fill);\n  grad.addColorStop(1, obj.stroke);\n  ctx.fillStyle = grad;\n  ctx.fill();\n  ctx.strokeStyle = obj.stroke;\n  ctx.lineWidth = obj.lineWidth;\n  ctx.stroke();\n  \n  // Brilho\n  ctx.beginPath();\n  ctx.ellipse(obj.x - obj.w * 0.2, obj.y - obj.w * 0.2, obj.w * 0.2, obj.w * 0.15, 0, 0, Math.PI * 2);\n  ctx.fillStyle = 'rgba(255,255,255,0.5)';\n  ctx.fill();\n  \n  // Texto da massa\n  ctx.fillStyle = '#ffffff';\n  ctx.font = 'bold ' + Math.max(11, obj.w * 0.55) + 'px monospace';\n  ctx.shadowBlur = 2;\n  ctx.fillText(obj.mass + ' kg', obj.x - 13, obj.y + 6);\n  \n  // Ponto de conexão da corda\n  ctx.beginPath();\n  ctx.arc(obj.x, obj.y - obj.w * 0.85, 5, 0, Math.PI * 2);\n  ctx.fillStyle = '#8b5cf6';\n  ctx.fill();\n  ctx.beginPath();\n  ctx.arc(obj.x, obj.y - obj.w * 0.85, 2.5, 0, Math.PI * 2);\n  ctx.fillStyle = '#facc15';\n  ctx.fill();\n  \n  ctx.restore();\n}",
      "update": "function(obj, dt) {}",
      "properties": [
        { "name": "mass", "label": "Massa (kg)", "type": "number", "default": 1, "min": 0.1, "max": 5, "step": 0.1 },
        { "name": "fill", "label": "Cor", "type": "color", "default": "#3b82f6" }
      ]
    }
  ],
  "tools": [
    {
      "id": "create_atwood",
      "name": "Criar Máquina Atwood",
      "icon": "science",
      "action": "function() {\n  const centerX = Lab.state.W / 2;\n  const pulleyY = 130;\n  const massStartY = 440;\n  \n  const existing = Lab.state.objects.find(o => o.pluginId === 'atwood_pulley');\n  if (existing) {\n    if (confirm('Já existe uma máquina. Deseja recriar?')) {\n      Lab.objects.remove(existing.id);\n      Lab.state.objects.filter(o => o.pluginId === 'atwood_mass').forEach(m => Lab.objects.remove(m.id));\n    } else { return; }\n  }\n  \n  const pulley = Lab.objects.add({\n    type: 'rect',\n    pluginId: 'atwood_pulley',\n    x: centerX - 24,\n    y: pulleyY - 24,\n    w: 48,\n    h: 48,\n    static: true,\n    leftMass: 1,\n    rightMass: 1.2,\n    leftPosition: massStartY,\n    rightPosition: massStartY\n  });\n  \n  const leftMass = Lab.objects.add({\n    type: 'circle',\n    pluginId: 'atwood_mass',\n    x: centerX - 95,\n    y: massStartY,\n    w: 28,\n    mass: 1,\n    fill: '#3b82f6'\n  });\n  \n  const rightMass = Lab.objects.add({\n    type: 'circle',\n    pluginId: 'atwood_mass',\n    x: centerX + 95,\n    y: massStartY,\n    w: 28,\n    mass: 1.2,\n    fill: '#ef4444'\n  });\n  \n  const pulleyObj = Lab.objects.get(pulley);\n  pulleyObj.leftMassObj = Lab.objects.get(leftMass);\n  pulleyObj.rightMassObj = Lab.objects.get(rightMass);\n  \n  alert('✅ Máquina de Atwood criada!\\n\\nMassa esquerda: 1.0 kg\\nMassa direita: 1.2 kg\\n\\nSelecione a roldana para ajustar os parâmetros.');\n}"
    },
    {
      "id": "adjust_masses",
      "name": "Ajustar Massas",
      "icon": "balance",
      "action": "function() {\n  const p = Lab.state.objects.find(o => o.pluginId === 'atwood_pulley');\n  if (!p) { alert('Crie a máquina primeiro.'); return; }\n  const m1 = parseFloat(prompt('Massa esquerda (kg):', p.leftMass));\n  const m2 = parseFloat(prompt('Massa direita (kg):', p.rightMass));\n  if (m1 > 0 && m2 > 0) {\n    p.leftMass = m1;\n    p.rightMass = m2;\n    if (p.leftMassObj) p.leftMassObj.mass = m1;\n    if (p.rightMassObj) p.rightMassObj.mass = m2;\n    alert('Massas ajustadas!');\n  }\n}"
    },
    {
      "id": "reset_atwood",
      "name": "Resetar",
      "icon": "restart_alt",
      "action": "function() {\n  const p = Lab.state.objects.find(o => o.pluginId === 'atwood_pulley');\n  if (!p) { alert('Crie a máquina primeiro.'); return; }\n  const startY = 440;\n  p.leftPosition = startY;\n  p.rightPosition = startY;\n  p.velocity = 0;\n  p.angularVelocity = 0;\n  p.rotationAngle = 0;\n  if (p.leftMassObj) p.leftMassObj.y = startY;\n  if (p.rightMassObj) p.rightMassObj.y = startY;\n  Lab.render.all();\n}"
    }
  ]
}